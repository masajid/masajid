<%= simple_form_for region do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :country_id, collection: Content::Country.all, label_method: :name, value_method: :id %>

  <%= f.button :submit, class: 'btn-primary' %>
<% end %>
