<div class='header'>
  <h1>
    <%= t('devise.mailer.reset_password_instructions.header', email: @resource.email) %>
  </h1>
</div>

<div class='content'>
  <div class='content__body'>
    <%= t('devise.mailer.reset_password_instructions.body.primary_html') %>
  </div>

  <div class='content__link'>
    <%= link_to t('devise.mailer.reset_password_instructions.cta_text'), edit_password_url(@resource, reset_password_token: @token) %>
  </div>

  <div class='content__body'>
    <%= t('devise.mailer.reset_password_instructions.body.secondary_html') %>
  </div>
</div>
